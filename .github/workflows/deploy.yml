name: Deploy MTCafeteria

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: mtcafeteria-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on:
      - self-hosted
      - cajuncloud-prod
    steps:
      - name: Deploy on production runner
        run: /home/lajicpajam/bin/mtcafeteria-deploy.sh
