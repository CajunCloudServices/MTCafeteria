#!/usr/bin/env bash
# Production `web` images compile Flutter inside Docker (see web/Dockerfile).
# Use `npm run flutter:web:sync` only if you run the Node host locally without
# rebuilding the image and need a bundle under public/flutter-web/.
set -euo pipefail
exit 0
